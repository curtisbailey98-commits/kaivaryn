import Link from "next/link";
import type { Metadata } from "next";
import { getPricingConfig, centsToDollars } from "@/lib/pricing";
import { formatCurrency } from "@/lib/utils";

export const metadata: Metadata = { title: "Pricing" };
export const dynamic = "force-dynamic";

const included = ["Revenue Recovery and Operations Efficiency workspaces", "Tenant-isolated data and role-based access", "Evidence-aware intelligence and honest empty states", "Approval-gated actions and auditable decisions", "CSV imports, exports, and implementation guidance"];

export default async function PricingPage() {
  const config = await getPricingConfig();
  const intro = formatCurrency(centsToDollars(config.introductoryPriceCents), config.currency);
  const standard = formatCurrency(centsToDollars(config.standardPriceCents), config.currency);
  return <><section className="relative overflow-hidden border-b border-neutral-900"><div className="relative mx-auto max-w-6xl px-4 py-16 sm:px-6 sm:py-24"><p className="public-kicker text-amber-400">Pricing that starts with the work</p><h1 className="mt-5 max-w-3xl text-4xl font-semibold leading-tight tracking-[-0.04em] text-white sm:text-6xl">Choose the engagement that matches your next decision.</h1><p className="mt-6 max-w-2xl text-base leading-7 text-neutral-400 sm:text-lg">Kaivaryn pricing is configuration-driven and capacity-aware. Start with a working session, then activate the operating model that fits your organization.</p></div></section><section className="mx-auto max-w-6xl px-4 py-16 sm:px-6 sm:py-24"><div className="grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><div className="public-card border-amber-500/30 p-7 sm:p-9"><div className="flex flex-wrap items-center justify-between gap-3"><p className="public-kicker text-amber-400">Introductory engagement</p><span className="rounded-full border border-amber-500/30 bg-amber-500/10 px-3 py-1 text-[10px] font-semibold uppercase tracking-[0.16em] text-amber-400">First {config.introductorySeats} clients</span></div><p className="mt-6 text-5xl font-semibold tracking-tight text-white">{intro}<span className="text-base font-normal text-neutral-500"> / month</span></p><p className="mt-4 max-w-md text-sm leading-6 text-neutral-400">Full platform access for early engagements while implementation capacity allows.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/demo" className="public-button-primary inline-block">Book executive working session <span aria-hidden>↗</span></Link><span className="inline-flex items-center text-xs text-neutral-500">Checkout unlocks after a completed demo.</span></div></div><div className="public-card p-7 sm:p-9"><p className="public-kicker">Standard platform</p><p className="mt-6 text-5xl font-semibold tracking-tight text-white">{standard}<span className="text-base font-normal text-neutral-500"> / month</span></p><p className="mt-4 text-sm leading-6 text-neutral-400">The ongoing platform price after introductory capacity is filled.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/demo" className="public-button-primary inline-block">Qualify for an engagement <span aria-hidden>↗</span></Link><Link href="/contact" className="public-button-secondary inline-block">Ask a question</Link></div></div></div><div className="mt-12 grid gap-10 lg:grid-cols-[.8fr_1.2fr]"><div><p className="public-kicker">Included in every engagement</p><h2 className="mt-3 text-2xl font-semibold text-white">The standard stays high at every tier.</h2></div><ul className="grid gap-3 sm:grid-cols-2">{included.map((item) => <li key={item} className="rounded-xl border border-white/[0.08] bg-white/[0.02] p-4 text-sm leading-6 text-neutral-300"><span className="mr-2 text-emerald-400">✓</span>{item}</li>)}</ul></div><div className="mt-10 flex flex-wrap items-center gap-3 rounded-xl border border-white/[0.08] bg-white/[0.02] p-4 text-sm"><span className="text-neutral-400">Already completed a Kaivaryn demo and payment?</span><Link href="/activate" className="text-amber-400 hover:text-amber-300">Continue to immediate onboarding →</Link></div><p className="mt-4 text-xs text-neutral-600">Stripe checkout is exposed only after a completed demo. Server-side Stripe webhook verification activates entitlements and moves the client directly into onboarding.</p></section></>;
}
